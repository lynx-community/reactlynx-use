# useBoolean

React state hook that tracks value of a boolean.

`useBoolean` is an alias for `useToggle`.

:::tip Thanks
Re-export from [react-use](https://github.com/streamich/react-use)
:::

## Usage

```tsx
import { useBoolean } from "@lynx-js/react-use";

const Demo = () => {
  const [on, toggle] = useBoolean(true);

  return (
    <view>
      <view>{on ? "ON" : "OFF"}</view>
      <view bindtap={toggle}>Toggle</view>
      <view bindtap={() => toggle(true)}>set ON</view>
      <view bindtap={() => toggle(false)}>set OFF</view>
    </view>
  );
};
```

## Type Declarations

```ts
declare function useBoolean(
  initialValue: boolean
): [boolean, (nextValue?: any) => void];
```
